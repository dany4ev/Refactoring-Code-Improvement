class User {
    constructor(name, email) {
        this.name = name;
        this.email = email;
    }

    save() {
        // Save user to database
    }

    sendEmail() {
        // Send email to user
    }

    generatePasswordResetToken() {
        // Generate password reset token
    }
}